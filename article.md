# 被消去的自由度去了哪里

### 从玻尔兹曼方程、蛋白质动力学，到序列模型的一条共同线索

---

## 引子：一个所有人都在做、但很少有人问代价的操作

科学工作里有一个几乎每天都在做、却极少被追问的动作：**扔掉自由度**。

做流体的人扔掉分子，只留速度场和压力场；做分子动力学的人扔掉水分子和侧链，只留 Cα 或几个反应坐标；做电磁仿真的人扔掉原子，只留 ε 和 μ；做语言模型的人扔掉说话者的全部意图与世界状态，只留 token 序列。我们把这个动作叫作粗粒化、约化、建模、抽象——名字不同，操作是同一个：**从一个高维的完整描述，投影到一个低维的、可计算的描述。**

这个动作的合法性，通常是靠"经验上管用"来辩护的。Navier–Stokes 管用，所以我们不问它凭什么;力场管用，所以我们不问被积掉的自由度去了哪里。

2026 年 7 月 23 日，国际数学联盟把菲尔兹奖颁给了邓煜，理由之一是"从稀薄气体的硬球动力学严格导出玻尔兹曼方程"。这项与 Zaher Hani、Xiao Ma 合作的工作，第一次在一个具体而完整的模型上，把这个动作的合法性证明到底。

我想说的是：**这件事的意义不止于气体动理学。它是一个更普遍问题的样板，而那个普遍问题，正横在今天的蛋白质动力学建模、乃至序列模型的架构选择前面。**

这篇文章要做的，是把三件表面上毫不相干的事串成一条线：

1. 邓煜等人证明了什么，为什么它难；
2. Mori–Zwanzig 形式主义如何把这件事推广成一条普遍定律：**被消去的自由度必然以"记忆"和"噪声"的形式回来**；
3. 这条定律对蛋白质粗粒化模型和序列模型意味着什么——以及一个我认为目前空着、而且可以被填上的位置。

---

## 一、玻尔兹曼方程的真正难点，不在方程里

玻尔兹曼方程长这样：

$$\partial_t f + v\cdot\nabla_x f = Q(f,f)$$

$f(t,x,v)$ 是单粒子分布函数：$t$ 时刻位于 $x$ 附近、速度在 $v$ 附近的粒子有多少。左边第二项描述粒子自由飞行，右边的碰撞算子 $Q(f,f)$ 描述碰撞如何重新分配速度。

这个方程之所以**有用**，唯一的原因是：它只含 $f$ 一个未知量，是封闭的。

但从牛顿力学出发，你能严格写下的并不是它，而是 BBGKY 层级：

$$\partial_t f^{(1)} \leftarrow f^{(2)} \leftarrow f^{(3)} \leftarrow \cdots \leftarrow f^{(N)}$$

单粒子分布的演化依赖两粒子联合分布，两粒子依赖三粒子，一路依赖到 $N$。这个层级是**精确的**，和牛顿力学完全等价——也正因如此它毫无用处，解它和解 $N$ 体问题一样难。

要把链条在第一层斩断，你必须做一步替换：

$$f^{(2)}(z_1,z_2)\;\approx\;f^{(1)}(z_1)\,f^{(1)}(z_2)$$

即"两个即将碰撞的粒子近似独立"（分子混沌假设）。碰撞项里那个二次型 $f\cdot f$ 就是从这里来的。

**所以要认清一件事：$Q(f,f)$ 的乘积结构不是物理定律，它是独立性假设的数学化身。** 玻尔兹曼方程成立与否，完全等价于这个独立性在动力学演化下能不能维持。

问题在于，粒子是靠碰撞相互作用的，而**每一次碰撞都在制造相关性**：A 撞过 B，B 后来撞 C，C 又可能撞回 A，于是三者携带了共同的碰撞历史。时间一长，碰撞历史组合爆炸，还会出现"环"（相关粒子重新相遇，即再碰撞）。相关性会不会越积越强，最终让独立性假设崩掉？

1975 年 Lanford 证明了这个推导成立——但只在平均每个粒子碰撞几次的**极短时间**内。此后近五十年，没人能跨过这道坎。

---

## 二、邓煜他们做对了什么：不假装相关性不存在

旧思路是尽力去证明 $f^{(k)}\approx f^{\otimes k}$，即任意有限个粒子都近似独立。长时间下这太粗糙了——粒子显然会产生大量关联。

邓煜、Hani、Ma 的策略是反过来的：

> **承认相关性一定会产生，把不同阶的相关性系统地编码下来（累积量展开），然后证明真正危险的高阶相关性总量仍然足够小。**

技术上，每一种碰撞历史被画成一张图（他们称为 molecules）：点是粒子或碰撞事件，边是碰撞与相关性，环代表再碰撞。于是连续动力学的难题被翻译成一个组合问题：

$$\underbrace{\text{危险碰撞图的数量}}_{\text{组合爆炸，很大}}\;\times\;\underbrace{\text{每张图发生的概率}}_{\text{很小}}\;\longrightarrow\;0$$

难点在于必须证明"小"确实压得过"大"。他们设计了一套切割算法，系统地拆解这些纠缠的碰撞图，把每一个环、每一个额外约束换算成概率小因子，最后把账结平。结论是：只要对应的玻尔兹曼方程还有正则解，推导就在**任意有限长时间**上成立。

这里有一个容易被忽略、但极其关键的物理图像：

> 独立性并不需要**永远**成立，只需要在**每次碰撞发生的那一刻、对即将相撞的那一对粒子**近似成立。

碰撞之后两个粒子当然强相关——它们的速度由同一次碰撞决定。这不要紧。要紧的是，在稀薄极限（Boltzmann–Grad 标度，$N\varepsilon^2\sim 1$）下，这对相关粒子几乎不会再次相遇，它们各自的下一个碰撞对象是"新鲜的"。**相关性不断被制造，又不断被稀释进广阔的相空间里失效。** 邓煜他们证明的，就是这场"制造—稀释"的收支平衡在长时间尺度上依然成立。

### 顺带澄清：这和大数定律不是一回事

很多科普会把它说成"粒子多了就平均稳定了"。这是错的，而且错得很关键。

经典大数定律的前提是**独立同分布**——独立性是白送的公理，定理只负责平均掉涨落，那是最容易的一步。而这里，独立性恰恰是**要被证明的结论**，因为粒子通过碰撞持续互动，持续制造记忆。

一个类比：大数定律说"一万个人各自抛硬币，正面比例趋于 1/2"。而这里的问题是"一万个人抛硬币，但每个人的硬币会根据碰到的其他人的结果发生物理形变，请证明正面比例仍趋于 1/2"。后者不是概率论习题，是动力系统问题。

另外两点差别也值得记住：大数定律的结论是**静态的一个数**（收敛到期望），而这里的结论是**动态的一条轨迹**（在整个时间区间上收敛到玻尔兹曼方程的解），这类结果叫尺度极限或**混沌传播**——"传播"二字点明了它有时间轴。而且这里唯一的随机性在初始条件，此后演化是完全确定论的牛顿力学；要证明的是确定性动力学作用在典型随机初值上，其粗粒化统计恰好等效于一个带不可逆性的方程。

所以真正的逻辑链不是

$$\text{粒子多}\Rightarrow\text{平均稳定}$$

而是

$$\text{粒子多，且彼此持续作用}\;\Rightarrow\;\text{相关性受控}\;\Rightarrow\;\text{宏观方程封闭}$$

中间那一步——**相关性受控**——就是五十年来无人跨过的鸿沟。

---

## 三、Mori–Zwanzig：这件事的普遍版本

现在把镜头拉远。玻尔兹曼是一个特例。一般情况下，当你从高维系统投影到低维变量时，会发生什么？

答案是 1960 年代 Zwanzig 和 Mori 给出的，我认为它是统计物理里最被低估的结果之一。

设完整微观状态 $X$（几十万维），你只保留一个粗粒化变量 $z=\xi(X)$。通过投影算子技术（定义 $P$ 投向"只依赖 $z$ 的部分"，$Q=1-P$ 投向正交补，再对演化算子 $e^{tL}$ 做 Dyson 恒等式拆分），$z$ 的演化可以被**恒等地**改写成广义 Langevin 方程：

$$M\ddot z(t) \;=\; \underbrace{-\nabla U_{\text{eff}}(z)}_{\text{有效力}} \;-\; \underbrace{\int_0^t K(t-s)\,\dot z(s)\,ds}_{\text{记忆}} \;+\; \underbrace{R(t)}_{\text{噪声}}$$

三项各有明确来源：

**有效力**：被消去的自由度的平衡态平均效应，即 potential of mean force。这就是粗粒化力场在做的事。

**记忆**：$z$ 现在怎么运动，不只取决于它现在在哪，还取决于它过去在哪。物理路径是：影响泄漏进快自由度，在快自由度里演化一圈，再返回来作用于 $z$。**被消去的自由度没有消失，它们变成了系统的记忆。** 这使得约化后的方程本质上是非马尔可夫的。

**噪声**：初始时刻快自由度的信息——你不知道也不想知道的那部分初值。对宏观观察者它表现为随机力，尽管在微观层面一切都是确定论的。

关键在于：**这不是近似，是恒等变形。任何粗粒化都精确地长这个样子。** 所有的建模艺术，都在于之后如何近似 $K$ 和 $R$。

### 还有一条免费赠送的约束

$$K(t)\;\propto\;\langle R(0)\,R(t)\rangle$$

记忆核等于噪声的自相关函数——第二类**涨落–耗散定理**（FDT）。

物理直觉很清楚：让布朗粒子减速的摩擦，和让它抖动的分子撞击，来自**同一批**微观碰撞，所以强度必然锁定。爱因斯坦 1905 年那个"扩散系数 ∝ 温度/摩擦系数"是它的雏形。Mori–Zwanzig 给出的是完全一般的版本：这不是巧合，而是"你扔掉了自由度"这一操作的结构性后果。

请记住这条约束，后面它是全文最有用的一句话。

### 于是可以重看邓煜的工作

BBGKY 层级本身就是一种 Mori–Zwanzig 结构：保留 $f^{(1)}$ 作慢变量，消去 $f^{(2)},f^{(3)},\dots$。按 MZ 定理，$f^{(1)}$ 的精确方程**必然含记忆项**——粒子的碰撞历史应该以记忆核的形式出现。

而玻尔兹曼方程里没有记忆项：$Q(f,f)$ 只依赖当下的 $f(t)$，是马尔可夫的。

所以用 MZ 的语言复述那项工作：

> **在 Boltzmann–Grad 极限下，硬球系统的记忆核在宏观尺度上塌缩为 δ 函数，涨落项消失，非马尔可夫的精确动力学退化为马尔可夫的玻尔兹曼方程。**

他们追踪的"碰撞历史图、环、再碰撞"，正是记忆核的组合展开；"相关性可控"就是"记忆足够短"。两套语言，同一件事。

区别在于：MZ 给的是**形式框架**（恒等式，永远对，但不告诉你记忆核有多大）；他们做的是在具体模型上**严格估计这个核**。后者才是难点。

判据也随之清晰：当快变量弛豫时间 $\tau_{\text{micro}} \ll$ 慢变量演化时间 $\tau_{\text{macro}}$ 时，记忆核近似 δ 函数，得到马尔可夫方程——玻尔兹曼、Navier–Stokes、普通 Langevin 都属于这一类**幸运案例**。没有尺度分离时，记忆不可忽略，你必须和非马尔可夫方程正面搏斗。

---

## 四、蛋白质：不幸运的那一类

现在换一个系统。

一个 300 残基的蛋白，加水加离子，几十万个原子。全原子 MD 的状态是 $X(t)=\{r_i,p_i\}$，几十万到百万维。但生物学上你真正关心的可能只是几个数：结构域开合角度、界面接触模式、是 folded 还是 unfolded、是 active 还是 inactive。

问题的形式和气体一模一样：

$$X(t)\;\longrightarrow\;z(t)\;\longrightarrow\;\text{某个封闭的低维演化方程}$$

按 MZ，答案必然是 GLE 的形式：有效力 + 记忆 + 噪声。

**但蛋白质和气体有一个致命的差别。**

在稀薄气体里，相关性最终**可以丢**——这正是可以推出玻尔兹曼方程的原因。

在蛋白质里，很多相关性**绝对不能丢**：

$$\text{helix A 转动}\;\to\;\text{界面改变}\;\to\;\beta\text{-sheet B 改变}\;\to\;20\,\text{Å 外的活性位点改变}$$

这就是**别构效应（allostery）**。蛋白质的功能，很多时候恰恰**就是**长程相关性本身。

所以对蛋白质，问题不是"怎样证明相关性消失"，而是：

> **哪些相关性会消失，哪些相关性构成决定宏观构象的慢模式？**

而且时间尺度分离在这里是不干净的。键长振动、水分子碰撞在 fs–ps；螺旋重排、结构域开合、配体结合、折叠在 ns–μs–ms。中间地带塞满了侧链翻转、loop 运动、水合壳弛豫——这些既不够快到可以塌缩成 δ 函数，也不够慢到值得显式保留。**记忆核的长尾正是从这里来的。**

### 这直接解释了粗粒化 MD 领域一个长期的尴尬

现在的机器学习粗粒化模型，绝大多数在拟合的是

$$M\ddot z = -\nabla U_\theta(z) + \text{noise}$$

也就是：**把复杂的记忆核吃进一个有效势里，然后配一个自由参数的 Langevin 热浴。**

结果是一个很有规律的现象：

- **热力学性质match 得不错**——自由能面、平衡分布、构象占比都能拟合上；
- **动力学性质系统性地错**——弛豫时间、转移速率、MFPT 常常差一个甚至几个数量级。

这不是调参不够，这是**结构性错误**。因为你扔掉的记忆核和你随手配的噪声之间，本该被 FDT 锁死，而现在它们互相不认识。平衡分布只由 $U_{\text{eff}}$ 决定，所以拟合得上；而 kinetics 由 $K$ 和 $R$ 共同决定，所以必然错。

**一句话：现在的 ML 粗粒化模型，学对了地形，学错了摩擦。**

---

## 五、于是建模目标要换：从"学分布"到"学传播子"

这是我认为最该被明确说出来的一句话。

今天主流的生成模型学的是

$$p(X)\qquad\text{——数据分布}$$

diffusion 和 flow matching 里确实有一个"时间"，但那是**算法时间**：噪声到数据的人造插值路径，与物理时间没有任何关系。结构生成模型回答的是"什么样的构象是合理的"。

MZ 指向的是另一个对象：

$$p(z_{t+\Delta t}\mid z_t,\,z_{t-\Delta t},\,\dots)\qquad\text{——演化算子（传播子）}$$

这里的 $t$ 是真实物理时间，条件里的历史项来自记忆核。

这两件事在数学对象、训练信号、评价指标上全都不同。分布模型的评价是结构合理性、能量、覆盖度；传播子模型的评价**必须是 kinetics**：implied timescales、MFPT、速率常数、转移矩阵的谱。用前者的指标去验收后者，是这个领域目前最常见的自欺。

于是整条技术路线变成：

$$\text{sequence}\;\to\;\text{energy landscape}\;\to\;\text{metastable states}\;\to\;\text{transition kinetics}\;\to\;\text{atomistic structures}$$

比现在绝大多数 sequence → single structure 的模型，多了整整一层。

---

## 六、旁支：为什么 Attention 是记忆核，SSM 是 Markovian embedding

这一节看似离题，其实是同一个定理的另一个投影。而且我认为这个对应比多数人以为的要紧。

**（1）语言模型本身就是一个粗粒化动力学问题。** 你观测到的是 token 序列，但产生它的系统的完整状态是说话人的意图、世界状态、未言明的上下文——全部是 bath，全部被投影掉了。按 MZ，**当你只观测系统的一部分自由度时，观测量的演化必然是非马尔可夫的**。所以"语言模型必须依赖长历史"不是工程经验，是 MZ 定理的直接推论；n-gram 的失败就是"扔掉记忆核"的失败。

**（2）Attention 字面意义上就是一个记忆核。**

$$\text{GLE:}\quad \int_0^t K(t-s)\,\dot z(s)\,ds \qquad\Longleftrightarrow\qquad \text{Attention:}\quad \sum_s \alpha(t,s)\,v(s)$$

$\alpha$ 就是 $K$，只不过它是数据依赖、非平稳的（$K(t,s)$ 而非 $K(t-s)$）。Transformer 保留全部历史、二次复杂度，对应"不做粗粒化、保留全部微观自由度"——精确但昂贵，相当于全原子 MD。

**（3）SSM（S4 / Mamba）就是 Markovian embedding。** 这个对应更严格。S4 的核心恒等式是：线性递归 $h'=Ah+Bu,\;y=Ch$ 等价于卷积 $y=K*u$，其中 $K(t)=Ce^{At}B$。而在统计物理里，若记忆核是指数和 $K(t)=\sum_i c_i e^{-\lambda_i t}$，则 GLE 精确等价于扩展变量空间中的一个马尔可夫系统——这叫 Markovian embedding，Mori 连分数展开是它的系统化版本。

**S4 的"卷积核 ↔ 递归状态"对偶性，和"GLE ↔ Markovian embedding"对偶性，是同一个数学事实。** Mamba 的 selectivity（让 $A,B$ 依赖输入）正好对应状态依赖的记忆核 $K(z_t,s)$。

于是 Transformer 与 SSM 之争，就是粗粒化里的经典权衡：

| | 序列模型 | 粗粒化动力学 |
|---|---|---|
| 保留全历史 | Transformer | 全原子 MD |
| 有限状态 + 记忆核 | SSM / RNN | GLE / CG model |
| 核心难点 | 状态够不够装下必要信息 | 慢变量选得对不对 |

**由此得到一个对实践很重要的推论：记忆长度和状态维度是可以互换的。** 慢变量 $z$ 选得足够完整，记忆核就短；$z$ 漏掉了关键慢模式，记忆核就长且难以参数化。所以"要不要显式建模 memory"这个问题，其实等价于"我的 collective variable 选够了没有"。

**不要一开始就纠结记忆核怎么参数化，先问慢变量对不对。**

---

## 七、诚实的边界：类比在哪里断裂

把一个框架用过头是最常见的智力事故，所以必须划清界限。

**LLM 没有平衡态、没有细致平衡、没有涨落–耗散定理。** 文本不是哈密顿系统，采样"温度"和物理温度毫无关系，采样噪声和模型的"摩擦"之间没有任何锁定关系。所以 FDT 这条最有价值的约束**不能**搬到语言模型上。

**LLM 的记忆是语义与逻辑的，不是涨落导致的。** MZ 的记忆核有明确的物理来源（bath 的自关联），attention 权重没有对应的守恒律或对称性来约束它。

所以我的判断是：

- **MZ → 蛋白质动力学**：这是真正的物理约束，能出硬结果；
- **MZ → 序列模型**：这是一个有启发的类比框架，能解释为什么必须非马尔可夫、为什么 attention 和 SSM 是自然的两种参数化，但它不会直接产出新架构。

同样地，也不要指望邓煜的切割算法能直接搬到蛋白质上——稀薄性、硬球、二体碰撞这些前提在浓稠的、有长程静电和溶剂介导相互作用的蛋白质体系里全部失效。可以迁移的是**问题的提法**，不是证明的技术。

---

## 八、那么，空着的位置在哪里

坦率地说，很多单项都不新：用神经网络学记忆核已有工作（数据驱动的 GLE、粗粒化摩擦核提取等）；"扩展变量把非马尔可夫变马尔可夫"是老结论，所以"引入 memory"本身不构成创新——它等价于加隐状态，而加隐状态是机器学习的默认操作。如果只是说"我在粗粒化模型里加了 attention"，审稿人会说：那不就是个 sequence model。

我认为真正空着、且可以被填上的位置有三个，按含金量排序：

### ① 把涨落–耗散定理作为硬约束

$$K(t)\;\propto\;\langle R(0)R(t)\rangle$$

这是目前 ML 粗粒化里几乎没人做对的地方。现状是学一个 $U_\theta(z)$ 再配一个自由的热浴，两者不匹配，于是热力学对、动力学错。

把 FDT 写进架构或损失函数，然后证明 **kinetics 指标**（不是自由能）确实变好——这是最可检验、最容易做成明确贡献的一点。

### ② 可迁移的动力学，而不是可迁移的结构

sequence → structure 已经接近饱和。sequence → **kinetics** 基本是空白：一个能在没见过的序列上给出正确速率常数的模型，价值远高于再一个结构生成器。

### ③ 慢变量的目标函数：预测，而不是重建

学 $z=E(X)$ 时，不要用 reconstruction loss，要用 predictive loss——最大化 $I(z_t;X_{t+\tau})$，即 VAMP / TICA / Koopman 那一族的思路。因为我们要找的是**动力学变量，不是压缩变量**。

这里有一个漂亮的呼应：**next-token prediction 之所以能学到好表示，正是因为它是预测目标而不是重建目标。** 同一个原理，在两个完全不同的领域各自被独立发现。

---

## 九、一个最小可行实验

理论说到这里，必须落到能做的事上。我会这样设计第一步：

**体系**：丙氨酸二肽，或一个已有长轨迹公开数据的快速折叠小蛋白。选它们的唯一理由是全原子参考答案已知且可靠。

**两个模型，唯一变量是记忆与 FDT**：

- 模型 A（对照）：无记忆，$M\ddot z=-\nabla U_\theta(z)+\text{自由参数噪声}$
- 模型 B：带记忆核 $K_\theta(z_t,s)$，且噪声协方差被 FDT 约束死

**评价指标（关键）**：不看自由能面拟合得多好——两个模型都会拟合得不错，这正是问题所在。只看：

- implied timescales 谱与全原子的一致性
- 主要构象转变的 MFPT
- 转移矩阵的本征值谱

如果模型 B 在 kinetics 上明显更好，而两者在热力学上没有差别，那就直接证明了论点：**记忆和 FDT 是动力学正确性的必要条件，而不是可选的精细化。** 这一张图就是一篇文章的核心。

后续的完整路线是清楚的：动力学编码器学慢变量（预测目标）→ 学有效力（保证平衡分布正确）→ 学记忆核 → 用 FDT 约束噪声 → 粗粒化轨迹 rollout → 必要时解码回全原子结构。

---

## 结语

把整篇文章压缩成一句话：

> **约化描述的代价是精确且普适的：你扔掉的每一个自由度，都会化作"记忆"与"噪声"回到方程里，而两者被涨落–耗散关系锁死。**

所有成功的宏观方程——玻尔兹曼、Navier–Stokes、普通 Langevin——都是**记忆恰好可以忽略的幸运案例**。而证明"记忆确实可忽略"，正是需要一枚菲尔兹奖来完成的工作量。

蛋白质不是那样的幸运案例。语言更加不是。

所以真正值得做的，不是继续问"这个蛋白长什么样"，而是问：

> **这个宏观对象，服从什么演化方程？它的慢变量是什么？被我扔掉的东西，以什么形式还在？**

这个提问方式，才是从邓煜的工作里真正能拿走的东西。

---

### 延伸阅读

- [Y. Deng, Z. Hani, X. Ma, *Long time derivation of the Boltzmann equation from hard sphere dynamics*](https://arxiv.org/abs/2408.07818), arXiv:2408.07818
- O. E. Lanford III (1975), 硬球动力学的短时间推导
- R. Zwanzig (1961); H. Mori (1965), 投影算子形式主义的两条原始路线
- A. J. Chorin et al., *Optimal prediction* —— MZ 在模型约化中的现代表述
- 粗粒化动力学综述：bottom-up coarse-graining 中 kinetics 难以保持的问题
- [VAMPnets: Deep learning of molecular kinetics](https://doi.org/10.1038/s41467-017-02388-1) / Markov State Models —— 慢变量与亚稳态的现代工具链
