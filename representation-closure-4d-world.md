# 从 4D 世界模型到蛋白质

### 一次关于表示、闭合与具身智能的跨领域对话

---

> **整理说明**：本文根据 2026 年 8 月 6 日的一场科技从业者交流整理。参与者分别具有三维视觉、视频生成、具身智能和蛋白质建模背景。文章删除了职业隐私、算力账户与企业内部信息，并对原始口语进行了结构化改写。

## 一、两个看似遥远的领域，遇到了同一个问题

一边是机器人和世界模型：如何让模型从图像理解空间、运动和物理因果。

另一边是蛋白质建模：如何从序列和静态结构理解多尺度几何、构象变化与功能。

它们看起来属于不同产业，却共享同一个根本困难：文本是一维序列，而真实世界是具有结构、尺度和动力学的高维对象。

因此，这场对话很快从具身智能创业和大模型工程，转向了一个更基础的问题：

> 什么样的 representation，能够保留真正决定未来的关系？

无论是机器人抓起杯子，还是蛋白质从一个构象切换到另一个构象，模型都不能只记住一张图或一组坐标。它必须知道哪些关系是稳定的，哪些变量会变化，以及动作或扰动之后系统将如何演化。

## 二、从一维文本走向 3D 和 4D，困难不只是数据变大

语言模型的成功容易让人产生一种直觉：只要把更多模态 token 化，再扩大数据和参数，能力就会自然出现。

但从文本走向三维和四维时，问题发生了变化。

文本中的词已经是高度粗粒化、相对稳定的单位。图像像素、三维点、视频帧和原子坐标则包含大量连续自由度。同一个物体换一个视角，底层输入可能完全不同；同一个蛋白质发生微小构象变化，功能可能改变，也可能完全不变。

模型必须处理：

- 旋转、平移和尺度；
- 遮挡与观察视角；
- 刚体、柔体、液体和光照；
- 多个时间尺度；
- 局部变化和全局语义之间的关系。

所以 3D、视频和 4D 世界模型的难点，并不是简单地比文本多几个维度，而是缺少天然稳定的离散单位。

## 三、“闭合”意味着找到可以自己演化的上层变量

交流中讨论了一个来自数学物理的概念：闭合。

假设微观系统状态为 (X_t)，我们构造一个粗粒化表示：

$$
Z_t=\Phi(X_t)
$$

如果只知道 (Z_t)，就能够在不重新展开全部微观自由度的情况下，近似预测未来：

$$
Z_{t+1}\approx G(Z_t,u_t)
$$

那么 (Z_t) 就接近一个闭合的宏观状态。

对机器人来说，这个状态不应只是当前图像的压缩，而应包含物体关系、接触状态、任务阶段和动作后果。

对蛋白质来说，它不应只是静态骨架坐标，而应包含结构层级、关键相互作用、构象状态以及功能相关的慢变量。

闭合提供了一条判断 representation 的标准：好的表示不是压缩率最高，也不是重建图像最清晰，而是能够支持正确的上层推理和未来演化。

## 四、蛋白质的多尺度表示：从残基点到有方向的椭球

蛋白质通常被表示为原子点、残基坐标或图结构。但点表示很难直接概括一个结构片段的方向与空间范围，原子级表示又可能给模型带来大量不必要计算。

对话中提出了一种层级表示：用椭球拟合不同尺度的结构对象。

椭球包含中心、朝向和三个尺度轴，可以表示：

- 单个残基的局部空间占据；
- 若干残基形成的结构片段；
- 一条链或一个结构域；
- 更高层级的整体形状。

这些对象同时进入 attention。语义通路计算普通的 (QK^\top)，几何通路则利用中心距离、方向、尺度和椭球重叠关系。马氏距离提供了一种自然度量：沿椭球长轴和短轴的位移具有不同意义。

这套思路的重点不是“椭球比点更真实”，而是它可能成为一种任务相关的粗粒化：让模型在残基、片段、链和整体之间直接交换信息。

如果它能够用更少自由度改善多链界面、减少 clash，或提高小样本抗原抗体任务的泛化，就说明这种表示保留了有用的宏观结构。

## 五、世界模型的关键不是生成视频，而是预测动作后果

具身智能中的世界模型常被视觉化为“预测下一帧”。但视频是否逼真并不是最终目标。

机器人真正需要知道的是：

- 夹爪闭合后是否抓住物体；
- 手臂抬起时杯子是否随之移动；
- 推动物体会不会碰到障碍；
- 当前动作是否推进了任务阶段。

因此，世界模型的价值在于学习：

$$
(Z_t,a_t)\rightarrow Z_{t+1}
$$

而不是单纯：

$$
I_t\rightarrow I_{t+1}
$$

高质量视频生成可以提供训练信号，却也可能把大量算力消耗在光影、纹理、烟雾和其他与动作决策无关的细节上。

这与蛋白质问题高度相似：模型不需要复现所有原子振动，而需要识别哪些构象变化影响结合与功能。两个领域都在寻找“足够真实但不必完整”的动力学表示。

## 六、为什么 4D 世界模型仍然很难

参与者曾从事 SLAM、三维重建、3D 生成和 4D 世界模型。其经验是，二维视频可以直接利用成熟生成模型处理纹理细节，但显式 3D/4D 表示必须同时维护空间一致性和时间一致性。

刚体运动只是最简单部分。柔体、布料、液体、烟雾、阴影和反射都会造成复杂变化。若只用二维视频监督三维世界，模型还面临不可观测区域和多解问题。

这使 4D 模型很容易陷入两个极端：

- 显式几何过强，细节和生成能力不足；
- 视频生成能力很强，却没有稳定、可操作的三维状态。

真正需要的可能是混合表示：保留可用于规划的显式空间变量，同时用生成模型补充难以建模的视觉细节。

## 七、具身智能为何要先跑小循环

对话中的产业判断相当务实：大多数创业团队不会一开始训练覆盖所有机器人的通用基础模型，而会先进入工业、物流、酒店等明确场景。

原因不是这些场景更“高级”，而是它们能够形成小循环：

$$
\text{部署}
\rightarrow
\text{收入}
\rightarrow
\text{真实数据}
\rightarrow
\text{模型改进}
$$

家庭场景更开放，任务和环境分布更复杂，短期内很难证明投入产出。工业场景虽然要求精度和稳定性，却可以限制工作空间、对象类型和动作集合。

对创业公司来说，先获得一个可运行场景，不只是商业策略，也是训练策略。真实部署会暴露仿真和 benchmark 看不到的问题，并持续产生最有价值的失败数据。

## 八、大模型竞争越来越像工程，但问题定义仍然稀缺

参与者认为，通用大模型训练已经具有很强的工程化特征。多卡训练、并行框架和基础配方逐渐成熟，一个公开方法被复现和追赶的速度极快。

如果团队只是在别人定义的问题上复制架构，优势很难长期保持。真正稀缺的是发现一个尚未被清楚表达、但可以被计算和验证的问题。

AI for Science 尤其如此。科学领域没有无限便宜的训练数据，评价也不能只靠人类偏好。一个新问题必须同时满足：

1. 科学对象有可计算表示；
2. 目标与真实机制或实验结果相关；
3. 结果能够被验证；
4. 计算成本低于它带来的信息价值。

这解释了为什么语言模型适合弥补信息差，却尚不能自动完成科学创新。创新的第一步往往不是回答，而是定义什么值得回答。

## 九、两个领域共享的研究路线

把整场对话压缩后，机器人世界模型与蛋白质建模可以写成同一个框架：

$$
\text{高维微观观测}
\xrightarrow{\Phi}
\text{任务相关表示 }Z
\xrightarrow{G}
\text{未来状态或生成结果}
$$

其中最重要的不是 (G) 有多大，而是 (Phi) 是否找到了正确变量。

对机器人，正确变量可能是对象、接触、局部坐标和任务阶段。

对蛋白质，正确变量可能是结构片段、界面、相互作用网络和构象慢模。

两者都需要对无关的微观扰动保持稳定，同时对真正改变任务结果的干预敏感。

这或许就是从 4D 世界模型走向蛋白质、又从蛋白质返回具身智能时，最值得保留的共同问题：

> 世界很高维，但智能不应被迫记住世界的全部细节。它必须找到那些能够自行演化、决定后果的变量。
